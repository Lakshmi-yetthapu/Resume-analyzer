# Existing Dependencies
streamlit
pandas
pillow
pytesseract
requests
pdfplumber
pymupdf
openpyxl
gdown
gspread
oauth2client

# New/Required for GitHub Screening & Robustness
urllib3
typing-extensions

# System-level dependencies (Note: these are usually installed via apt-get, not pip,
# but kept here for your reference as per your list)
# tesseract-ocr
# tesseract-ocr-eng